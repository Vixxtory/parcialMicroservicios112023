server:
  port: ${PORT:8888}

spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Vixxtory/parcialMicroserviciosConfig/tree/main
          #default-label: master
